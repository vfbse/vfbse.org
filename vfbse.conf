{
  email => {
    server => "-- the server address --",
    port => 587,
    user => "-- the smtp user name --",
    pass => "-- the smtp password --",
    to => "-- the recipient --",
    cc => "-- send some guy a copy here --"
  }
};
